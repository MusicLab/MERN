html
  head 
    meta(charset="UTF-8")
    title='My Aplicación con Sockets'
    script(src="/socket.io/socket.io.js")
    script(src="main.js")
    

  body 

  if miDataDinamica.response.length
    head
      title="primer pug"
      link(rel="stylesheet" href="../../style.css")
    body
      table.table.table-striped
        tr
          th Nombre
          th Precio
          th Foto
        each val in miDataDinamica.response
          tr
            td  #{val.name}
            td  #{val.price} 
            td 
              img(src= val.thumbnail, style='width:80px;height:80px;')
  else 
    p "No hay productos"      